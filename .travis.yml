language: php
sudo: required
dist: bionic

addons:
  mariadb: '10.3'
  hosts:
    - moodle.test
  firefox: latest
  # postgresql: "9.4"
  apt:
    packages:
    #  - oracle-java9-installer
    #  - oracle-java9-set-default
      - redis-server

services:
  - memcached
  - redis-server

cache:
  directories:
    - $HOME/.composer/cache
    - $HOME/.npm

env:
  global:
    - IGNORE_PATHS=vendor,source
    - IGNORE_NAMES=*.txt,*.md

# Alternate tests with MySQL and PostgreSQL
matrix:
  include:
    - php: 7.3
      env: DB=mariadb MOODLE_BRANCH=MOODLE_38_STABLE plgdir=moodle/availability/condition/ipaddress
    #- php: 7.3
    #  env: DB=pgsql MOODLE_BRANCH=master plgdir=moodle/availability/condition/ipaddress

before_install:
  - phpenv config-rm xdebug.ini
  - mv $plgdir/* .
  - rm -rf moodle
  - cd ../..
  - nvm install 8.9;
    nvm use 8.9;
    composer create-project -n --no-dev --prefer-dist moodlerooms/moodle-plugin-ci ci ^2;
  - export PATH="$(cd ci/bin; pwd):$(cd ci/vendor/bin; pwd):$PATH"
install:
  - moodle-plugin-ci install -vvv
  #- moodle-plugin-ci install
  - echo '$CFG->cookiesecure = false;' >> moodle/config.php

script:
  - moodle-plugin-ci validate
  - moodle-plugin-ci phplint
  - moodle-plugin-ci phpcpd
  - moodle-plugin-ci phpmd
  - moodle-plugin-ci codechecker
  - moodle-plugin-ci savepoints
  - moodle-plugin-ci mustache
  - moodle-plugin-ci grunt || false
  - moodle-plugin-ci phpunit
  - moodle-plugin-ci behat
